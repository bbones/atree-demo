import {createLocalVue, mount} from '@vue/test-utils';
import ATree from "@bbones1967/atree";

describe("ATree", () => {
    test("Render ATree", async () => {
        const wrapper = await mount(ATree);
    })
})